[Unit]
Description=Influx Container
Requires=docker.service
After=docker.service

[Service]
User=pi
Restart=always
ExecStart=/usr/bin/docker start -a influx
ExecStop=/usr/bin/docker stop -t 2 influx
Type=simple	

[Install]
WantedBy=power-monitor.service
